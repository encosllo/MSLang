import Mslang.Regular

/-!
# `Mscong.Recognizable` -- recognizability calculus (MSCong §2.3)

Reuses `Mslang`'s recognizability: a language is recognizable when its
cogenerated congruence has finite index (`Mslang.IsRegularLanguage`,
`Mslang.IsFiniteIndex`, `Mslang.congCogenerated`).

This module is a scaffold for milestone **M1** (OpenSpec change
`add-mscong-project`); the calculus (`Rec(A)`, `Rec_s(A)`, `δ^{s,L}`, the
Boolean/product/subdirect closures, `(lf,s)`-recognition) is added there.
-/

namespace Mscong

end Mscong
