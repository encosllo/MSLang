import Mslang.Term

/-!
# `Mscong.Substitution` -- substitution, iteration, and quotients

Reuses `Mslang`'s free algebra and terms (`Mslang.Term`, `Mslang.Free`) and
congruences/quotients (`Mslang.Congruence`).

Scaffold for milestone **M3** (OpenSpec change `add-mscong-project`):
substitution, iteration, and quotient operators, and the recognizability
results `PRecSubs` / `PRecQ`.
-/

namespace Mscong

end Mscong
