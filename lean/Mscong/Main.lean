import Mscong.Recognizable
import Mscong.BasicTerms
import Mscong.Substitution
import Mscong.TreeHom

/-!
# `Mscong.Main` -- recognizability theorems

The recognizability theorems of "Congruence based proofs of the recognizability
theorems for free many-sorted algebras": `PRecVar`/`PRecConst`/`PRecOp` (M2),
`PRecSubs`/`PRecQ` (M3), `PRecH`/`PRecLH`/`PRecILH` (M4).

Scaffold for the `Mscong` project (OpenSpec change `add-mscong-project`); the
theorems themselves are added one milestone at a time.
-/

namespace Mscong

end Mscong
