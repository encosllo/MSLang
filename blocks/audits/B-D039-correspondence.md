# Correspondence audit transcript -- `B-D039`

Protocol: Architecture.md Section 11.2, two-stage blind. First audit of this
block's correspondence layer, run in Session 119 in section batches (one fresh
agent per stage-batch; no agent was told the expected answer). Stage 1 saw only
the Lean declarations and their definitions (with definition bodies); stage 2 saw
only the stage 1 read-back and the contract.

- **Lean declarations:** `Mslang.syntacticCongruence` (`lean/Mslang/Translation.lean`)
- **Contract:** Definition `B-D039`, section "Congruence cogenerated  by an $S$-sorted subset of the underlying $S$-sorted set of a $\Sigma$-algebra.".
- **Outcome:** `equivalent`
- **Recorded as:** `E-000363`
- **Encoding:** dependent-type carrier model, `representation/pilot-encoding.md`.
- **Independence:** the stage agents share this session's underlying model
  (`deepseek-v4.1-flash`); shared blind spots are not excluded.

## Stage 1 -- read-back (fresh agent, Lean only)

> # B-D039 — read-back
>
> ## Ambient setup (abbreviations in the file)
>
> - `SSet S := S → Type u`.
> - `Signature S := List S × S → Type u`.
> - `SortedEqv A := ∀ s, Setoid (A s)`: an `S`-sorted family of setoids
>   (`Setoid (A s) = ⟨rel, refl, symm, trans⟩`).
> - `Sub A := ∀ s, Set (A s)`.
> - `Alg Sig` is used but not defined here. Its carrier is `A.1 : SSet S`; `A.2`
>   interprets symbols: `A.2 (w, s) σ` takes one element of `A.1 (w.get k)` for each
>   `k : Fin w.length` and returns an element of `A.1 s`.
>
> ## Definitions
>
> ### `IsElemTranslation (Sig) (A) (t s : S) (T : A.1 t → A.1 s) : Prop`
>
> `T` is an *elementary translation* from `t` to `s` iff there exist a word
> `w : List S`, an index `i : Fin w.length` with `hwit : w.get i = t`, a symbol
> `σ : Sig (w, s)`, and fillers `a k h : A.1 (w.get k)` for all `k ≠ i`, such that
> **for all** `x : A.1 t`,
>
> ```
> T x = A.2 (w, s) σ (fun k =>
>         if h : k = i then (cast along w.get i = t, reversed) x
>         else a k h)
> ```
>
> Existentials outermost, `∀ x` inside: one fixed `(w, i, hwit, σ, a)` works for all
> inputs. `T` is `σ` with `x` substituted into the `i`-th slot (transported across
> `w.get i = t`) and fixed elements elsewhere.
>
> ### `TlGen (Sig) (A) : (t s : S) → (A.1 t → A.1 s) → Prop` (inductive)
>
> The translation closure, smallest family of maps closed under:
>
> - `refl (t) : TlGen Sig A t t id`;
> - `elem {T} : IsElemTranslation Sig A t s T → TlGen Sig A t s T`;
> - `comp {T : A.1 t → A.1 u} {U : A.1 u → A.1 s} :
>     TlGen Sig A t u T → TlGen Sig A u s U → TlGen Sig A t s (U ∘ T)`.
>
> ### `congCogenerated (Sig) (A) (L : Sub A.1) : SortedEqv A.1`
>
> The *cogenerated* sorted equivalence determined by the subobject `L`. At each sort
> `t` its underlying relation is
>
> ```
> x ~_t y  :⟺  ∀ (s : S) (T : A.1 t → A.1 s),
>                 TlGen Sig A t s T → (T x ∈ L s ↔ T y ∈ L s)
> ```
>
> i.e. `x ~_t y` iff every translation `T` out of `t` (in the `TlGen` sense) is
> either in `L s` at both `T x` and `T y` or at neither. The bundled equivalence is
>
> - reflexivity: all cases give `Iff.rfl`;
> - symmetry: pointwise `(h s T hT).symm`;
> - transitivity: pointwise `(h1 s T hT).trans (h2 s T hT)`.
>
> ### `syntacticCongruence (Sig) (A) (L : Sub A.1) : SortedEqv A.1` (abbrev)
>
> A name alias for the cogenerated relation:
>
> ```
> syntacticCongruence Sig A L := congCogenerated Sig A L
> ```
>
> So the "syntactic congruence" is, definitionally, exactly `congCogenerated`
> above.
>
> ## Theorems / lemmas
>
> None. The file contains the abbrev `syntacticCongruence`, `IsElemTranslation`,
> the inductive `TlGen`, `congCogenerated`, and abbrevs. The equivalence laws are
> built inline in `congCogenerated`; no separate proposition is asserted.

## Stage 2 -- comparison (fresh agent, read-back + contract only)

> # B-D039 verdict
>
> Contract: the relation `Ω^A(L)` from the preceding definition is *called* the
> congruence on `A` cogenerated by `L` (equivalently, the syntactic congruence on `A`
> determined by `L`). The clause is purely terminological, attaching the names
> "cogenerated congruence" / "syntactic congruence" to `Ω^A(L)`.
>
> - name for `Ω^A(L)` ↔ `syntacticCongruence Sig A L := congCogenerated Sig A L`,
>   which is definitionally the `Ω^A(L)` relation (`congCogenerated`). Match.
> - no further mathematical content is asserted by the contract clause, and none is
>   added beyond the alias.
>
> Contract clauses with no Lean counterpart: none
>
> Verdict: equivalent — the Lean abbrev assigns exactly the name the contract
> introduces to the same cogenerated relation.

