# Correspondence audit transcript -- `B-R019`

Protocol: Architecture.md Section 11.2, two-stage blind. Both stages were run by
fresh subagents with isolated contexts; neither was told the expected answer.

- **Lean declaration:** `Mslang.syntacticCongruence_eq_congCogenerated`
  (`lean/Mslang/Translation.lean`).
- **Contract:** Remark `B-R019`: the syntactic (principal) congruence of `L`
  falls under the notion of congruence cogenerated by `L`.
- **Outcome:** `equivalent`
- **Encoding:** dependent-type carrier model, `representation/pilot-encoding.md`.
- **Independence:** both stages share this session's underlying model
  (`deepseek-v4.1-flash`); shared blind spots are not excluded.

## Stage 1 -- read-back (fresh agent, Lean only)

> `congCogenerated Sig A L` identifies `x, y` of sort `t` exactly when every
> term-translation `T : A_t → A_s` maps them into `L_s` together or outside it
> together; it is the congruence on `A` cogenerated by the language `L`.
> `syntacticCongruence Sig A L` is an abbreviation whose right-hand side is
> literally `congCogenerated Sig A L`, so the asserted equality is definitional.

## Stage 2 -- comparison (fresh agent, read-back + contract only)

> **Outcome: `equivalent`.**
>
> The contract states that the syntactic (principal) congruence of `L` is the
> congruence cogenerated by `L`; the read-back formalizes exactly this as the
> definitional identity `syntacticCongruence = congCogenerated`. The many-sorted
> setting is the direct generalization of the single-sorted semigroup/monoid
> case, so the statement captures the contract's claim without adding or
> omitting content.

## Residual note

This is a naming/definitional identification (`B-D039` already names
`congCogenerated` the syntactic congruence); the theorem records it as a mapped
object. The record inherits the pilot-encoding residuals (`carrier-model`,
`small-large`, `univalence-missing`).
