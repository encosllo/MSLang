# Correspondence audit transcript -- `B-P027`

Protocol: Architecture.md Section 11.2, two-stage blind. Both stages were run by
fresh subagents with isolated contexts; neither was told the expected answer.
Stage 1 saw only the Lean declarations and their definitions (no manuscript, no
project history). Stage 2 saw only the stage 1 read-back and the contract.

- **Lean declaration:** `Mslang.congCogenerated_le_transPreimage`
  (`lean/Mslang/Translation.lean`).
- **Contract:** Proposition `B-P027` (`TAntiTrans`), section "Congruence
  cogenerated by an `S`-sorted subset ...".
- **Outcome:** `equivalent`
- **Recorded as:** `E-000155`
- **Encoding:** dependent-type carrier model, `representation/pilot-encoding.md`.
- **Independence:** both stages share this session's underlying model
  (`deepseek-v4.1-flash`); shared blind spots are not excluded.

## Stage 1 -- read-back (fresh agent, Lean only)

> `congCogenerated_le_transPreimage Sig A {T} (hT : TlGen Sig A t s T) L :
> sortedEqvLe (congCogenerated Sig A L) (congCogenerated Sig A (transPreimage T L))`.
> `transPreimage T L = deltaSub t (Set.preimage T (L s))` is `T⁻¹[L]`
> (component `T⁻¹[L_s]` at sort `t`, empty elsewhere). Proof: unfold the
> refinement and introduce a further translation `V : A_t' → A_s''`; if
> `s'' = t`, the goal reduces to the `Ω^A(L)` clause at the composite
> translation `T ∘ V` (`TlGen.comp`); if `s'' ≠ t`, both sides are `∅` and the
> clause is trivial.

## Stage 2 -- comparison (fresh agent, read-back + contract only)

> **Outcome: equivalent.**
>
> Hypotheses match exactly: `A`, `L ⊆ A`, `t, s ∈ S`, and `T ∈ Tl_t(A)_s` as
> `hT : TlGen Sig A t s T`. `transPreimage T L` is the concentrated inverse
> image `(T⁻¹[L])_t = T⁻¹[L_s]`, empty elsewhere. The conclusion is
> `Ω^A(L) ⊆ Ω^A(T⁻¹[L])` in the correct direction. No dropped/added hypothesis,
> no weakening/strengthening, no size mismatch (the case split on `s'' = t` is
> exactly the concentration/emptiness dichotomy).

## Residual note

The correspondence is `equivalent` relative to the pilot encoding
(`representation/pilot-encoding.md`, independently audited in `E-000040`,
residuals `carrier-model`, `small-large`, `univalence-missing`). Those residuals
are inherited by this verdict; a representation change (class C6) stales it.
